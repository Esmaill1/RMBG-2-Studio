# CPU-only Requirements
#
# IMPORTANT: torch and torchvision must be installed SEPARATELY before this file,
# using the CPU-only PyTorch index URL. Run:
#
#   pip install torch torchvision --index-url https://download.pytorch.org/whl/cpu
#
# Then install the remaining dependencies with:
#
#   pip install -r requirements.txt

# Core ML
transformers>=4.39.1,<4.50.0
timm
kornia
devicetorch

# Image processing
pillow
opencv-python-headless
numpy
scikit-image

# Web framework
flask>=3.0.0
werkzeug

# Utilities
fsspec
huggingface-hub
pydantic